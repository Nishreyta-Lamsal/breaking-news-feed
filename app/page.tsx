import React, { useState } from 'react'

const page = () => {

  const [articles, setArticles] = useState([]);
  const [pendingArticles, setPendingArtciles] = useState([]);
  return (
    <div>page</div>
  )
}

export default page